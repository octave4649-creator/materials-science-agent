---
title: "方向三·材料科学文献驱动的科学发现智能体 · 整体开发指导文档"
category: "development-guide"
tags: [开发指导, 材料科学, 文献Agent, GOAI, 赛道三, 路线A]
description: "面向「材料科学文献驱动的科学发现智能体」赛题的整体开发指导：赛题理解、总体架构、技术选型、分阶段开发路线图、各模块实现详解、评测迭代与提交准备"
created: "2026-08-04"
updated: "2026-08-04"
status: "published"
version: "1.0"
author: "参赛团队"
---

# 方向三·材料科学文献驱动的科学发现智能体 · 整体开发指导文档

## 0. 文档导读

本文档是方向三赛题的**总体作战手册**，回答「做什么、怎么做、按什么顺序做、做到什么程度」。配套文件：

| 文件 | 作用 |
|------|------|
| `.trae/rules/01-competition-rules.md` | 赛题规则与评审标准速查 |
| `.trae/rules/02-literature-data-sources.md` | 文献数据源接入手册 |
| `.trae/rules/03-materials-databases.md` | 材料数据库接入手册 |
| `.trae/rules/04-literature-agent.md` | 基本任务（文献调研 Agent）设计 |
| `.trae/rules/05-route-a-SPR.md` | 路线 A：构效关系发现 |
| `.trae/rules/06-route-b-simulation.md` | 路线 B：模拟方法创新 |
| `.trae/rules/07-route-c-synthesis.md` | 路线 C：合成路线与工艺设计 |
| `.trae/rules/08-frontier-research.md` | 前沿研究与基准 |
| `.trae/rules/09-toolchain.md` | 工具链与环境搭建 |
| `.trae/rules/10-submission-templates.md` | 三阶段提交材料模板 |

**阅读建议**：初赛准备按「第 1→2→3→4 节」顺序推进；进入复赛后重点精读「第 5 节路线 A」与「第 6 节评测迭代」；提交前核对「第 8 节提交物与合规」。

## 1. 赛题理解（先想清楚再动手）

### 1.1 一句话理解

本方向考察：**能否构建一个「从文献出发、产出可验证科学发现」的智能体系统**——先靠文献调研 Agent 找到研究空白（Research Gap），再在进阶路线中把 Gap 变成可验证的科学发现，且每一步都有可审计的证据链。

### 1.2 赛题结构

```
方向三：材料科学文献驱动的科学发现智能体
├── 基本任务（必做，占材料方向评分 50%）
│   └── 文献调研 Agent：检索筛选 → 知识抽取 → Gap 识别 → 报告生成
└── 进阶路线（三选一，占 50%）
    ├── 路线A：构效关系发现（搜索算法 × LLM 深度融合 + 数据库交叉验证）
    ├── 路线B：模拟方法创新（DFT/MD/MC 加速 + ML 势函数 + benchmark）
    └── 路线C：合成路线与工艺设计（逆向合成 × LLM + 工艺优化）
```

**关键提示**：基本任务产出物是进阶路线的输入。因此**前期投入的重心应放在把文献调研 Agent 做扎实**，而不是急着做路线部分——一个高质量的知识库 + Research Gap 清单，能让路线 A/B/C 事半功倍。

### 1.3 评审解码（决定投入优先级）

| 评审维度 | 权重 | 对材料的实际含义 | 投入建议 |
|---------|------|-----------------|---------|
| 技术性能 | 45% | 材料方向内「基本任务 50% + 进阶路线 50%」综合分 | 两条腿都要硬 |
| 科学意义 | 30% | 发现的 Gap/构效关系是否真实、是否超出已有边界 | 选对细分领域极关键 |
| 方法创新性 | 20% | 搜索方法与 LLM 是否深度融合（不是简单集成） | 路线 A 的核心得分点 |
| 开源贡献 | 5% | 代码、模型、管线开源质量与可复用性 | 复赛前补齐 |

**核心推论**：
1. **路线 A 是最大公约数**：路线 A 与基本任务共享「文献 → 知识库 → 数据库」链路，不需要额外实验/算力硬件，且「搜索×LLM 融合」与「科学意义」两个高分维度天然契合。**本方案默认选择路线 A**。
2. **科学意义靠选题**：赛题明确说「避免似是而非、无实质价值的低质量文本」，宁可深耕一个细分领域（热电/催化/电池/固态电解质），也不要泛泛而谈。
3. **证据链是通吃要求**：基本任务的 Gap、路线 A 的构效关系、报告交叉引用，全部要求可审计证据链——这是贯穿始终的工程红线。

## 2. 总体技术架构

### 2.1 系统全景图

```
┌─────────────────────────────────────────────────────────────────┐
│                   材料文献科学发现智能体（Agent 系统）              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  基本任务层（文献调研 Agent）                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 检索Agent │→│ 抽取Agent │→│ 分析Agent │→│ 报告Agent │        │
│  │ (检索筛选) │  │ (知识抽取) │  │(Gap识别) │  │(报告生成) │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
│       │              │              │                           │
│  ┌────┴─────┐   ┌────┴─────┐   ┌───┴─────┐                     │
│  │Sciverse  │   │MinerU+LLM│   │知识库+向量│                     │
│  │MCP Server│   │结构化抽取 │   │检索RAG   │                     │
│  └──────────┘   └──────────┘   └──────────┘                     │
│                                                                 │
│  进阶路线层（路线 A：构效关系发现）                                │
│  ┌──────────────────────────────────────────────────┐          │
│  │  搜索循环：LLM×搜索算法深度融合                        │          │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐   │          │
│  │  │LLM 生成 │→│搜索算法  │→│LLM 评估  │→│LLM 剪枝  │   │          │
│  │  │候选种子 │ │(GA/MCTS│ │科学合理性│ │聚焦空间  │   │          │
│  │  │        │ │/BO/SR) │ │        │ │          │   │          │
│  │  └────────┘ └────────┘ └────────┘ └──────────┘   │          │
│  │        ↓                                          │          │
│  │  ┌────────────────────────────────────┐           │          │
│  │  │ 数据库交叉验证：MP / OQMD / NOMAD    │           │          │
│  │  └────────────────────────────────────┘           │          │
│  └──────────────────────────────────────────────────┘          │
│                                                                 │
│  基础支撑层                                                    │
│  ├─ LLM 接入：OpenAI / 国产大模型 / 本地模型                    │
│  ├─ Agent 编排：LangGraph（状态机 + 条件路由 + 证据链）          │
│  ├─ 数据层：Sci-Base 本地语料 + 向量库（chroma/qdrant）         │
│  └─ 证据链：统一审计日志（检索记录/DOI/页码/调用时间）            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 架构设计原则

1. **证据链优先**：所有 Agent 的调用都落在统一审计日志中，每个结论可回溯到「来源 + DOI + 页码 + 调用时间」。这是基本任务与路线 A 的共同硬要求。
2. **四 Agent 流水线**：检索/抽取/分析/报告职责解耦，便于独立评测与迭代；复赛阶段可用 LangGraph 状态机编排，支持条件路由与人工介入（HITL）。
3. **知识库驱动**：抽取结果落库（JSON/Parquet + 向量索引），既服务基本任务的 Gap 识别，也服务路线 A 的候选生成——避免两套重复采集。
4. **轻量起步**：初赛用最少工具跑通 MVP（Sciverse MCP + LLM + 简单搜索），复赛再上 LangGraph 多 Agent 与完整搜索循环。

### 2.3 技术选型建议

| 层级 | 技术 | 理由 |
|------|------|------|
| Agent 编排 | LangGraph / LangChain | 状态机、条件路由、HITL、checkpoint，支持复杂科研工作流 |
| 文献检索 | Sciverse MCP Server | 官方推荐；调用记录天然构成可审计证据链 |
| 文档解析 | MinerU（pipeline 后端起步） | 官方推荐；PDF→Markdown，公式/表格结构化 |
| 本地语料 | Sci-Base（material 子集） | 2500 万+ OA 文献，CC-BY-4.0，本地批处理 |
| 向量检索 | chroma / qdrant + bge-m3 | 轻量、支持多语言 |
| 材料数据 | mp-api + pymatgen | 路线 A 数据库交叉验证主力 |
| 搜索算法 | DEAP(GA) / 自研 MCTS / skopt(BO) / gplearn(SR) | 与 LLM 深度融合的载体 |
| LLM | OpenAI API / 国产大模型（按资源选择） | 证据评估与假设生成的「大脑」 |
| 可视化 | Streamlit / Gradio | 初赛方案展示、复赛 demo |

## 3. 开发路线图（分阶段）

### 3.1 三阶段总览

| 阶段 | 时间 | 目标 | 核心交付物 |
|------|------|------|-----------|
| 初赛 | 7.16–8.16 | 跑通 MVP + 定选题 | 方案说明文档、技术路线、可行性验证 |
| 复赛 | 8.25–9.3 | 完整系统 + 实验结果 | 可运行代码仓库、实验报告、依赖披露 |
| 决赛 | 9.22 | 路演展示 | 海报、demo、项目一页纸 |

### 3.2 初赛阶段（7.16–8.16）：MVP 与选题

**第 1 周：环境与数据接入（基础）**
- [ ] 注册 Sciverse API Key，跑通 `sciverse semantic-search` / `sciverse search` CLI
- [ ] 搭建 Python 环境（见 `.trae/rules/09-toolchain.md` 第 5 节）
- [ ] 安装 MinerU，解析 3-5 篇材料文献 PDF 验证管线

**第 2 周：文献调研 Agent 原型（核心）**
- [ ] 实现检索 Agent：Sciverse agentic-search + meta-search 组合
- [ ] 实现抽取 Agent：LLM + schema 约束抽取成分/结构/性能/方法/条件
- [ ] 实现 Gap 识别初版：覆盖率分析 + 矛盾检测 + LLM 推理
- [ ] 实现报告生成：结构化 Markdown 报告模板

**第 3-4 周：选题收敛 + 路线 A 预研**
- [ ] 用调研 Agent 跑 2-3 个候选细分领域，比较 Gap 质量
- [ ] 确定主攻领域（如热电材料/固态电解质/催化）
- [ ] 注册 Materials Project API，跑通 mp-api 查询
- [ ] 验证 LLM × 搜索算法结合的最小闭环（候选生成→评估→筛选）

**第 5 周：初赛材料撰写**
- [ ] 按 `10-submission-templates.md` 第 2 节撰写方案说明文档
- [ ] 附初步实验截图 / 检索示例 / MVP demo 视频

### 3.3 复赛阶段（8.25–9.3）：完整系统与实验

- [ ] 用 LangGraph 重构为多 Agent 状态机（含 HITL 审核节点）
- [ ] 完善证据链审计：统一日志 + 报告自动引用
- [ ] 路线 A 完整搜索循环：GA/MCTS/BO/SR × LLM 深度融合
- [ ] 数据库交叉验证：MP/OQMD 验证候选构效关系
- [ ] 完成 benchmark/自建评测集上的量化结果
- [ ] 撰写实验报告、依赖披露、开源仓库整理

### 3.4 决赛阶段（9.22）：展示打磨

- [ ] 海报：问题→方法→结果→科学意义四段式
- [ ] 现场 demo：现场演示一次「问题→Gap→构效关系→数据库验证」全流程
- [ ] 项目一页纸：聚焦方法、核心结果与科学意义

## 4. 模块开发详解

### 4.1 文献检索模块（检索 Agent）

**功能**：根据研究问题自主检索并筛选文献。

**实现要点**：
1. **问题拆解**：把研究问题拆成可检索子问题，逐个发起检索
2. **双通道检索**：语义检索（agentic-search，找证据片段）+ 结构化筛选（meta-search，按年份/期刊/学科过滤）
3. **字段引导**：先用 meta-catalog 确认可用字段，避免 LLM 编造不存在的筛选条件
4. **证据链记录**：每次调用记录查询词、时间戳、返回 doc_id、DOI

**关键代码骨架**：

```python
import asyncio
from sciverse import AgentToolsClient

async def search_literature(query: str, top_k: int = 5) -> list:
    async with AgentToolsClient() as client:
        # 语义检索，返回可引用证据片段
        semantic = await client.semantic_search(query, top_k=top_k)
        # 结构化筛选（可选：按年份/期刊限制）
        meta = await client.search_papers(
            query=query, year_from=2020, page_size=10
        )
        return semantic, meta
```

### 4.2 知识抽取模块（抽取 Agent）

**功能**：从文献中提取成分、结构、性能、模拟方法、合成条件等结构化信息。

**实现要点**：
1. **MinerU 预处理**：PDF → Markdown（表格转 HTML、公式转 LaTeX），再交给 LLM
2. **Schema 约束**：用 JSON Schema 限定输出字段（见 `.trae/rules/04-literature-agent.md` 第 3.2 节）
3. **多轮校验**：抽取结果回查原文片段，防止幻觉
4. **归一化去重**：同一材料体系别名、同构异名统一

**知识库落库**：每条记录 = 材料知识四元组（成分-结构-性能-条件）+ 证据链接 + 向量化索引。

### 4.3 Research Gap 识别模块（分析 Agent）

**功能**：识别未被充分探索的方向、矛盾结论、缺失知识连接。

**四种 Gap 识别方法**：

| 方法 | 原理 | 适用 |
|------|------|------|
| 覆盖率分析 | 成分×结构×性能矩阵找空白格 | 未探索方向 |
| 矛盾检测 | 同一体系多文献数值对比标记冲突 | 矛盾结论 |
| 连接发现 | 知识图谱/共现分析找弱关联 | 缺失知识连接 |
| LLM 推理 + 验证 | LLM 提假设，Sciverse 回查确认 | 全部类型 |

**评估要求**：Gap 需附证据链、区分新颖性、说明可操作性（能否转化为路线 A 的搜索种子）。

### 4.4 报告生成模块（报告 Agent）

**功能**：输出结构化调研报告或文献综述。

**报告结构**（详见 `10-submission-templates.md` 第 3.1 节）：研究问题与范围 → 检索策略与数据来源 → 知识抽取结果 → Research Gap 清单（每个带证据链+交叉引用）→ 文献综述 → 结论建议 → 附录文献清单。

### 4.5 路线 A：搜索算法 × LLM 深度融合（进阶核心）

**赛题红线**：搜索方法须与 LLM 深度融合，而非仅用 LLM 生成搜索代码。这是方法创新性（20%）的主战场。

**三种融合角色**：

| 角色 | 作用 | 示例 |
|------|------|------|
| 假设种子生成器 | LLM 生成候选假设作搜索种群种子 | 基于 Gap 生成候选掺杂体系/结构假设 |
| 科学合理性评估器 | LLM 评估中间结果合理性 | 打分：热力学可行性、物理合理性、文献支持度 |
| 搜索空间引导器 | LLM 根据反馈剪枝/聚焦搜索空间 | 淘汰低潜力方向，聚焦高潜力区间 |

**推荐闭环**：

```
1. LLM 生成初始候选假设集合（基于 Research Gap 与知识库）
2. 搜索算法（GA/MCTS/BO/SR）在候选空间迭代探索
3. LLM 评估中间结果科学合理性 → 过滤不合理候选
4. LLM 分析搜索反馈 → 剪枝/聚焦
5. 收敛后：LLM 生成构效关系解释 + 文献对照
6. 数据库交叉验证（MP/OQMD/NOMAD）
7. 输出：构效关系清单（陈述+证据链+新知/已知区分+机制解释）
```

**算法选型建议**（可多选组合）：
- **符号回归（SR）**：产出可解释解析表达式，天然契合「构效关系」目标——参考 Composition-Weighted Symbolic Regression（MCTS-GP 混合，arXiv:2605.02267）
- **MCTS**：LLM 作为节点扩展器/价值评估器，适合序贯决策——参考 AI4S-SDS 稀疏 MCTS
- **遗传算法（GA）**：组合材料空间（掺杂/组分）全局搜索，DEAP 实现
- **贝叶斯优化（BO）**：连续参数空间高效采样，skopt/optuna 实现

**参考系统**：CheMatAgent（HE-MCTS）、dZiner（逆向设计）、MatAgent（详见 `.trae/rules/08-frontier-research.md`）。

### 4.6 数据库交叉验证模块（路线 A 必备）

- Materials Project（mp-api + pymatgen）：带隙/生成能/能带
- OQMD：热力学稳定性/相图
- NOMAD：高精度数据复现

**验证流程**：文献抽取 → 数据库查询 → 一致性检查 → 新颖性判断（库中有文献未谈=Gap；库中缺失=潜在新发现）→ 证据链记录。

## 5. 初赛方案撰写要点

初赛核心交付物是方案说明文档（≤4 页），评审据此判断是否进入复赛。**写作优先级高于代码**——初赛不强制提交代码。

### 5.1 方案文档结构（对应 4 页）

| 页 | 内容 | 关键论证 |
|----|------|---------|
| P1 | 问题定义：所选细分领域 + 真实未解问题 | 用文献/数据说明问题真实存在、无公认答案 |
| P2 | 技术方案：文献调研 Agent + 路线 A 架构 | 架构图 + 各模块技术选型 |
| P3 | 方法与创新点：搜索算法 × LLM 融合设计 | 突出融合深度（生成/评估/引导三角色） |
| P4 | 可行性验证 + 预期成果 + 风险 | 附 MVP demo / 检索示例 / 初步结果 |

### 5.2 选题自检（避免翻车）

- [ ] 问题是否**具体可证伪**（不是「设计更好的材料」这类空泛目标）
- [ ] 是否能用文献综述/数据缺口**证明问题悬而未决**
- [ ] 细分领域是否有**公开数据支撑**（MP/OQMD 可查）
- [ ] 构效关系是否能**给出科学解释**（不是黑箱）
- [ ] 团队是否有该领域**基本背景**（材料/化学/物理）

## 6. 评测与迭代

### 6.1 自建评测体系（基本任务）

| 评测对象 | 指标 | 方法 |
|---------|------|------|
| 检索质量 | 检索相关性、覆盖率 | 人工标注小集 + NDCG@K |
| 抽取质量 | 字段级 F1、准确率 | 对照人工标注的抽取结果 |
| Gap 识别 | 准确率、新颖性、可操作性 | 专家评分 + Sciverse 回查确认 |
| 报告质量 | 结构化程度、证据链完整率 | 检查清单打分 |

### 6.2 自建评测体系（路线 A）

| 评测对象 | 指标 | 方法 |
|---------|------|------|
| 搜索效率 | 找到有效构效关系的代数/时间 | 固定预算对比基线 |
| 发现质量 | 数据库验证通过率、新知比例 | MP/OQMD 交叉验证 |
| 融合深度 | LLM 参与环节数、剪枝有效性 | 消融实验（有/无 LLM 引导） |
| 可解释性 | 机制解释合理性 | 材料专家评分 |

### 6.3 迭代节奏

1. **周迭代**：每周跑通一个模块 + 记录评测指标
2. **消融实验**：关键设计（LLM 引导开关、算法选型）做对比，支撑复赛报告
3. **失败记录**：记录无效 Gap/被否定的候选假设——负结果在科学意义评审中也有价值（说明探索是真实的）

## 7. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| Sciverse API 配额/费用 | 检索受阻 | 优先用免费额度；Sci-Base 本地语料兜底 |
| LLM 幻觉（伪造证据/DOI） | 证据链失效 | 强制 schema + 原文回查 + 证据链审计 |
| 选题过泛 | 科学意义得分低 | 初赛第 3-4 周强制收敛单一细分领域 |
| 搜索算法与 LLM 融合流于表面 | 创新性得分低 | 提前做消融实验证明融合价值 |
| 时间不足 | 交付不完整 | 优先级：MVP > 证据链 > 路线 A 深化 > 打磨 |
| 数据库 API 限额 | 验证受阻 | OQMD/NOMAD 多源互补，必要时小样本验证 |

## 8. 提交物与合规（赛前必查）

### 8.1 三阶段提交清单

| 阶段 | 截止 | 必交 | 自查 |
|------|------|------|------|
| 初赛 | 8.16 | 方案说明文档、技术路线 | 4 页内、问题真实、方案清晰 |
| 复赛 | 9.3 | 代码仓库、实验报告、依赖披露 | 可运行、可复现、种子固定 |
| 决赛 | 9.22 | 海报、demo、一页纸 | 聚焦方法与科学意义 |

### 8.2 合规红线

- [ ] 商业 API/闭源模型披露：调用环节、费用假设、可替代性、对复现性的影响
- [ ] 外部数据授权：Sci-Base（CC-BY-4.0）、MP/OQMD 使用条款
- [ ] 基于已有项目需说明来源、贡献范围、新增创新点
- [ ] 代码仓库：README、环境配置、随机种子、LICENSE、依赖版本

### 8.3 时间红线提醒

| 节点 | 时间 |
|------|------|
| 初赛提交截止 | **8.16**（距今约 12 天） |
| 复赛入围公布 | 8.24 |
| 复赛提交截止 | 9.3 |
| 决赛 | 9.22 |

## 9. 立即行动清单（本周）

1. **今天**：注册 Sciverse API Key，跑通 `sciverse semantic-search` CLI（10 分钟）
2. **本周内**：搭建 Python 环境 + MinerU 解析 2 篇材料 PDF
3. **本周内**：用调研 Agent 跑一个候选领域（如热电材料）的文献检索与 Gap 初筛
4. **下周前**：确定主攻细分领域，注册 Materials Project API
5. **第 5 周前**：完成初赛方案文档初稿

---

## 附：关键资源速查

| 资源 | 地址 |
|------|------|
| Sciverse 文档 | https://sciverse.opendatalab.com/docs |
| Sciverse-Agent-Tools | https://github.com/opendatalab/Sciverse-Agent-Tools |
| Sci-Base | https://huggingface.co/datasets/opendatalab/Sci-Base |
| MinerU | https://github.com/opendatalab/MinerU |
| Materials Project | https://materialsproject.org |
| OQMD | http://oqmd.org |
| NOMAD | https://nomad-lab.eu |
| pymatgen | https://pymatgen.org |

> 相关文档：[[知识库导航]]、[[赛题规则与要求]]、[[基础任务·文献调研 Agent]]、[[路线A·构效关系发现]]
