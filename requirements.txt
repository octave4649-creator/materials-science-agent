# 核心依赖（与 pyproject.toml [project.dependencies] 同步维护）
sciverse>=0.11.0
httpx>=0.27
pydantic>=2.5
python-dotenv>=1.0
ruff>=0.5
pytest>=8.0
pytest-asyncio>=0.23

# 可选依赖组（按需安装，见 README「安装步骤」）
# extraction: MinerU 文档解析
mineru[all]>=3.4
# validation: pymatgen / mp-api 数据库验证
pymatgen>=2024.0
mp-api>=0.40
# search: 搜索算法增强
scikit-learn>=1.3
pymoo>=0.6
deap>=1.4
